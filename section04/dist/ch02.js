/**
 * 함수 타입 표현식
 * - 함수 타입을 타입 별칭과 함께 별도로 정의할 수 있다
 */
const add = (a, b) => a + b;
const sub = (a, b) => a - b;
const multiply = (a, b) => a * b;
const divide = (a, b) => a / b; // 이렇게도 쓸 수 있음
const add2 = (a, b) => a + b;
const sub2 = (a, b) => a - b;
const multiply2 = (a, b) => a * b;
const divide2 = (a, b) => a / b;
export {};
// add2();
// add2.name
